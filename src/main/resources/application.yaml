spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:postgresql://localhost:5432/traveldb
    username: postgres
    password: test
    driverClassName: org.postgresql.Driver